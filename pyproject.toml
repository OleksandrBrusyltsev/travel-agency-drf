[project]
name = "travel-agency"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1.5",
    "django-unfold>=0.47.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9.4",
]
